# Automate

The data cleaning is handled in a separate script, which is great. However, it is inside of a folder when it should be a standalone script in the 214final project. That script should also contain the write_csv line to write a csv into another folder called output.

The R folder contains two scripts Functions and moving-average. There only needs to be one which contains the code for moving averages. Currently the moving-average.R script contains more code than the function requires, but the Functions.R script contains the function code.

The code inside day1scratch.R runs without any errors, great!

# Organize

The 4 csv's containing the data all are in the data folder, separate from everything else which is good. 

As afformentioned, there still needs to be an output folder created to house the final data csv to be created in the cleandata script. 

The function for finding moving average (which I think exists in the Functions.R and cleandata.R scripts) is not yet applied in the scratch code or cleandata.R file. The moving average function will need to be used inside the clean data script to create the final csv. 

There are some scripts that could be deleted when finished or added to the scratch folder, namely Functions.R or moving-average.R, whichever will not contain the final moving average function definition.

# Document

Title is decriptive. Could probably remove the titles of the README secions from the title. 

The description of the repository's purpose is great and descriptive!

The repo contents section is good as well. I assume there will be descriptions of all the final files once it is more finished.

Having a link to access the data is good. Maybe including further instructions on downloading could be helpful?

In the references, the data citation needs to be included with a link in addition to the publication, which is already present.

Line spacing in scripts is a bit inconsistent. I would standardize the format, using AIR, and keep spacing consistent.

